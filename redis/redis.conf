# redis/redis.conf

bind 0.0.0.0
port 6379
timeout 300
tcp-keepalive 60

# Enable persistence
save 900 1
save 300 10
save 60 10000
appendonly yes

# Optional: Set a password
# requirepass MyStrongPassword123
